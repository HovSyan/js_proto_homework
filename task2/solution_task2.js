/**
 * NO SOLUTION IS PROVIDED. TRY IT YOURSELF
 *
 * HINT: you can save the result state in class itself. and then return this.result in getValue().
 *
 * function Calculator(initialValue) {
 *   ....
 *   if(initialValue == null) {
 *     this.result = 0;
 *   } else {
 *     this.result = initialValue;
 *   }
 *   ....
 * }
 * */
